# Installable, trimmed version of requirement.txt.
#
# Differences from requirement.txt:
#   - numpy 1.23.5 -> 1.26.4   : faiss-cpu 1.9.0 requires numpy>=1.25, so the original
#                                pin makes the file unresolvable. 1.26.4 also satisfies
#                                torch 2.1 (needs numpy<2) and pandas 2.2.
#   - faiss-gpu removed        : faiss-cpu and faiss-gpu install the same `faiss` module.
#                                The code only calls faiss.read_index / CPU search.
#   - h5py, jsonlines added    : imported by src/database.py and src/rag.py but missing.
#   - unused packages dropped  : llama-index*, llama-parse, nltk, pypdf, beautifulsoup4,
#                                striprtf, openpyxl, sqlalchemy, scipy, scikit-learn,
#                                langchain / langchain-openai / langchain-anthropic,
#                                langsmith, python-dotenv, pydantic-settings, click, ...
#                                (nothing under code/ imports them)
#   - huggingface_hub pinned   : transformers 4.44 needs <1.0 and sentence-transformers
#                                2.7 breaks on hub 1.x.

torch==2.1.0
transformers==4.44.2
tokenizers==0.19.1
sentence-transformers==2.7.0
accelerate==0.33.0
safetensors==0.4.4
huggingface_hub==0.25.2

faiss-cpu==1.9.0
numpy==1.26.4
pandas==2.2.2

langchain-core==0.3.12
langchain-community==0.3.0
langchain-huggingface==0.1.0
langchain-text-splitters==0.3.0

tinydb==4.8.0
tiktoken==0.7.0
h5py
jsonlines

openai==1.52.0
anthropic==0.34.2
requests==2.32.3
httpx==0.27.0
tqdm==4.66.4
